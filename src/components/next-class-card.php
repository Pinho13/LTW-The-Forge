<section class="next-class-card">
    <div class="next-class-content">
        <p class="section-label"><?= htmlspecialchars($sectionLabel) ?></p>
        <h2><?= htmlspecialchars($classTitle) ?></h2>
        <p><?= htmlspecialchars($classMeta) ?></p>
    </div>

    <a href="<?= htmlspecialchars($buttonHref) ?>" class="btn-primary">
        <?= htmlspecialchars($buttonText) ?>
    </a>
</section>